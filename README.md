# iphoto-sifter
Finds photos in a folder and adds missing photos to iPhoto library

## Installation

### Prerequisites
- Ensure you have access to an iPhoto library (macOS)

### Steps

1. **Clone the repository**
   ```bash
   git clone https://github.com/toddwprice/iphoto-sifter.git
   cd iphoto-sifter
   ```

2. **Install dependencies**
   ```bash
   # Dependencies will be specified once the application is implemented
   ```

3. **Run the application**
   ```bash
   # Usage instructions will be added once the application is implemented
   ```
